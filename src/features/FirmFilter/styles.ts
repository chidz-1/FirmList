import { styled } from "styled-components";

export const FirmFilterWrapper = styled.div`
	flex: 1 0 auto;

	div {
		display: flex;
	}

	label {
		font-weight: bold;
		flex: 1 0 auto;
		text-align: start;
	}

	input {
		margin-right: 0.5rem;
	}
`;
